services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: skillswap
      POSTGRES_USER: skillswap
      POSTGRES_PASSWORD: skillswap_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025" # SMTP — Laravel sends mail here
      - "8025:8025" # Web UI — you view "sent" emails here in a browser

  server:
    build:
      context: ../server/laravel
      dockerfile: ../../docker/Dockerfile.server
    ports:
      - "8000:8000"
    env_file:
      - ../server/laravel/.env
    depends_on:
      - postgres
      - redis
      - mailhog
    volumes:
      - ../server/laravel:/var/www/html

  reverb:
    build:
      context: ../server/laravel
      dockerfile: ../../docker/Dockerfile.server
    command: php artisan reverb:start --host=0.0.0.0
    ports:
      - "8080:8080"
    env_file:
      - ../server/laravel/.env
    depends_on:
      - postgres
      - redis
    volumes:
      - ../server/laravel:/var/www/html

  client:
    build:
      context: ../client/nextjs
      dockerfile: ../../docker/Dockerfile.client
    ports:
      - "3000:3000"
    env_file:
      - ../client/nextjs/.env.local
    depends_on:
      - server
    volumes:
      - ../client/nextjs:/app
      - /app/node_modules # prevents Windows node_modules from overwriting container's

volumes:
  postgres_data:
  redis_data:
